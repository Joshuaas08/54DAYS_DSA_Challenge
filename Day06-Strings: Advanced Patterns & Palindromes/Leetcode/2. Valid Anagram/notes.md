Anagrams contain the same characters with the same frequency.
Use an integer array of size 26 for lowercase English letters.
Increment counts for s.
Decrement counts for t.
Every value should end at 0.

Time: O(n)
Space: O(1)

Key Takeaway:
When character frequency matters, think Frequency Counting.
